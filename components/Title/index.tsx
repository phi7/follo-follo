import { NextPage } from "next";

const Title: NextPage = () => {
  return (
    <div className="flex flex-row justify-center">
      <div className="text-4xl font-raleway font-semibold">フォロ×フォロ</div>
    </div>
  );
};

export default Title;
